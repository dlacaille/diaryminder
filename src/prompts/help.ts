const help = [
  "Sorry, I don't recognize this option",
  "`!skip` Skip this prompt",
  "`!help` Print out this message",
  "`!init` Go through initial setup again",
].join("\n")

export default help
